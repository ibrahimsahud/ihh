# 🚀 دليل رفع مشروع IHH على الإنترنت - خطوة بخطوة

## 📋 الملخص السريع

**المشكلة:** مشروعك يحتوي على:
- ✅ Frontend (HTML/CSS/JS) في مجلد `public/`
- ✅ Backend (Node.js + Express) في `server.js`
- ✅ Database (MS SQL Server)

**الحل:** استخدام **Render.com** للتطبيق الكامل + **Azure SQL** للقاعدة

---

## 🎯 الحل الموصى به (100% مجاني)

### المنصات المستخدمة:
1. **Render.com** - للباك إند والفرونت إند معاً
2. **Azure SQL Database** - لقاعدة البيانات

---

## 📝 الخطوة 1: تحضير المشروع

### أ) التأكد من وجود جميع الملفات

لقد قمت بإنشاء الملفات التالية:
- ✅ `.gitignore` - لحماية الملفات الحساسة
- ✅ `.env.example` - مثال على متغيرات البيئة
- ✅ `render.yaml` - إعدادات Render
- ✅ تعديل `server.js` و `database.js` لدعم متغيرات البيئة

### ب) اختبار المشروع محلياً

```bash
# تأكد أن كل شيء يعمل
npm start
```

افتح: http://localhost:3000

---

## 🌍 الخطوة 2: رفع الكود على GitHub

### 1. إنشاء Repository جديد

اذهب إلى: https://github.com/new

- **اسم المستودع:** ihh-charity-system
- **الوصف:** نظام إدارة المساعدات الخيرية
- **Public أو Private:** اختر Public
- **لا** تضف README أو .gitignore (موجودين بالفعل)

### 2. رفع الكود

افتح Terminal في VS Code واكتب:

```bash
# تهيئة Git (إذا لم يكن موجوداً)
git init

# إضافة جميع الملفات
git add .

# Commit أول
git commit -m "Initial commit: IHH Charity Management System"

# تعيين الفرع الرئيسي
git branch -M main

# ربط المستودع (غيّر username بـ اسمك)
git remote add origin https://github.com/ibrahimsahud/ihh-charity-system.git

# رفع الكود
git push -u origin main
```

**ملاحظة:** عند السؤال عن Username وPassword:
- Username: اسم المستخدم في GitHub
- Password: استخدم **Personal Access Token** (ليس كلمة المرور العادية)

#### كيف تحصل على Token؟
1. اذهب إلى: https://github.com/settings/tokens
2. اضغط "Generate new token" → "Generate new token (classic)"
3. اختر: `repo` (full control)
4. اضغط "Generate token"
5. **انسخ** الـ Token واحفظه (لن تراه مرة أخرى!)

---

## 💾 الخطوة 3: إنشاء قاعدة بيانات مجانية

### الطريقة 1: Azure SQL Database (الأفضل)

#### أ) إنشاء حساب Azure

1. اذهب إلى: https://azure.microsoft.com/free/
2. اضغط "Start free"
3. سجّل باستخدام حساب Microsoft (أو أنشئ واحد جديد)
4. **ستحتاج:** بطاقة ائتمان للتحقق (لن يتم الخصم!)
5. احصل على **$200 رصيد مجاني** + خدمات مجانية لمدة 12 شهر

#### ب) إنشاء SQL Database

1. اذهب إلى: https://portal.azure.com
2. ابحث عن "SQL databases" في شريط البحث
3. اضغط "+ Create"

**الإعدادات:**

**Basics:**
- Subscription: Free Trial
- Resource group: أنشئ جديد "IHH-Resources"
- Database name: `IHH_Hayir`
- Server: اضغط "Create new"
  - Server name: `ihh-db-server` (يجب أن يكون فريداً)
  - Location: West Europe (الأقرب)
  - Authentication: SQL authentication
  - Admin login: `ihhdbadmin`
  - Password: أنشئ كلمة مرور قوية (احفظها!)

**Compute + storage:**
- اضغط "Configure database"
- اختر "Basic" (أرخص خيار - حوالي $5/شهر)
- أو اختر "DTU-based" → "Basic" (5 DTUs)

**Networking:**
- Connectivity: Public endpoint
- Firewall rules:
  - ✅ Allow Azure services to access this server
  - ✅ Add current client IP address

4. اضغط "Review + create"
5. اضغط "Create"

**انتظر 5-10 دقائق حتى ينتهي الإنشاء**

#### ج) الحصول على Connection String

بعد إنشاء قاعدة البيانات:

1. اذهب إلى قاعدة البيانات
2. من القائمة اليسرى: "Connection strings"
3. انسخ "ADO.NET" connection string
4. ستبدو هكذا:
```
Server=tcp:ihh-db-server.database.windows.net,1433;Initial Catalog=IHH_Hayir;Persist Security Info=False;User ID=ihhdbadmin;Password={your_password};MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;
```

**احفظ:**
- Server: `ihh-db-server.database.windows.net`
- Database: `IHH_Hayir`
- User: `ihhdbadmin`
- Password: كلمة المرور التي أنشأتها

---

### الطريقة 2: FreeSQLDatabase.com (بديل مجاني تماماً)

1. اذهب إلى: http://www.freesqldatabase.com/freesql/
2. املأ النموذج:
   - Database Name: `IHH_Hayir`
   - Description: IHH Charity Management System
3. ستتلقى email بمعلومات الاتصال:
   - Server
   - Database Name
   - Username
   - Password

---

## 🔄 الخطوة 4: رفع الـ Database Schema

### أ) تحميل Azure Data Studio

1. حمّل من: https://docs.microsoft.com/sql/azure-data-studio/download
2. ثبّته وافتحه

### ب) الاتصال بقاعدة البيانات

1. اضغط "New Connection"
2. املأ المعلومات:
   - Server: `ihh-db-server.database.windows.net`
   - Authentication type: SQL Login
   - User name: `ihhdbadmin`
   - Password: كلمة المرور
   - Database: `IHH_Hayir`
   - Encrypt: True
3. اضغط "Connect"

### ج) تشغيل SQL Scripts

1. افتح ملف `CREATE_TABLES.sql`
2. اضغط "Run" أو F5
3. **مهم:** تأكد من نجاح جميع الأوامر

**الترتيب الصحيح:**
```sql
-- 1. حذف الجداول القديمة (إن وجدت)
-- 2. إنشاء الجداول الجديدة
-- 3. إضافة البيانات الأساسية
```

---

## 🚀 الخطوة 5: النشر على Render.com

### أ) إنشاء حساب Render

1. اذهب إلى: https://render.com
2. اضغط "Get Started"
3. سجّل باستخدام **GitHub** (الأسهل)
4. امنح Render الإذن للوصول إلى مستودعاتك

### ب) إنشاء Web Service

1. من Dashboard، اضغط "New +" → "Web Service"
2. اختر المستودع: `ihh-charity-system`
3. إذا لم يظهر:
   - اضغط "Configure account"
   - امنح Render الوصول للمستودع

**الإعدادات:**

- **Name:** `ihh-system`
- **Region:** Frankfurt (الأقرب للشرق الأوسط)
- **Branch:** `main`
- **Root Directory:** (اتركه فارغاً)
- **Environment:** `Node`
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- **Plan:** **Free** (مهم!)

### ج) إضافة Environment Variables

اضغط "Advanced" → "Add Environment Variable"

أضف **جميع** هذه المتغيرات:

```
DB_USER=ihhdbadmin
DB_PASSWORD=كلمة_المرور_التي_أنشأتها
DB_SERVER=ihh-db-server.database.windows.net
DB_NAME=IHH_Hayir
DB_ENCRYPT=true
DB_TRUST_CERT=true
NODE_ENV=production
PORT=10000
```

**ملاحظة:** PORT=10000 مهم لـ Render

### د) النشر

1. اضغط "Create Web Service"
2. انتظر 3-5 دقائق
3. Render سيقوم بـ:
   - ✅ استنساخ الكود من GitHub
   - ✅ تثبيت المكتبات (`npm install`)
   - ✅ تشغيل السيرفر (`npm start`)

### هـ) الحصول على الرابط

بعد النجاح، ستحصل على رابط مثل:
```
https://ihh-system.onrender.com
```

**افتح الرابط وتأكد من عمل التطبيق! 🎉**

---

## 🔧 الخطوة 6: اختبار التطبيق

### اختبارات مهمة:

1. **الصفحة الرئيسية:**
   ```
   https://ihh-system.onrender.com
   ```

2. **صفحة الإدارة:**
   ```
   https://ihh-system.onrender.com/admin-login.html
   ```

3. **API Endpoints:**
   ```
   https://ihh-system.onrender.com/api/beneficiaries
   https://ihh-system.onrender.com/api/donations
   ```

### إذا ظهرت أخطاء:

1. **انظر إلى Logs:**
   - في Render، اضغط "Logs"
   - ابحث عن رسائل الخطأ

2. **أخطاء شائعة:**
   - ❌ "Cannot connect to database"
     - تأكد من: DB_SERVER صحيح
     - تأكد من: Firewall يسمح بـ Azure services
   
   - ❌ "Login failed for user"
     - تأكد من: DB_USER و DB_PASSWORD صحيحين

---

## 🎨 الخطوة 7: (اختياري) GitHub Pages للفرونت إند فقط

**ملاحظة:** هذا **فقط** إذا أردت فصل الفرونت عن الباك إند

### المشكلة:
GitHub Pages يدعم HTML/CSS/JS الثابتة فقط، لا يدعم Node.js

### الحل:
استخدم **Render** لكليهما (كما فعلنا) ✅

---

## ⚡ نصائح مهمة

### 1. الأمان:
- ✅ **لا** ترفع `.env` على GitHub (محمي في `.gitignore`)
- ✅ استخدم Environment Variables في Render
- ✅ غيّر كلمات المرور الافتراضية

### 2. الأداء:
- ⚠️ الخطة المجانية على Render:
  - تتوقف بعد 15 دقيقة من عدم النشاط
  - أول طلب بعد التوقف يأخذ 30-60 ثانية
  - **الحل:** استخدم خدمة ping مثل UptimeRobot

### 3. قاعدة البيانات:
- 💡 احتفظ بنسخة احتياطية من البيانات
- 💡 Azure Basic tier: 2GB مساحة
- 💡 لا تنسَ إيقاف الخدمات إذا لم تعد تستخدمها

### 4. التحديثات:
```bash
# لتحديث التطبيق:
git add .
git commit -m "وصف التحديث"
git push

# Render سيعيد النشر تلقائياً!
```

---

## 🆘 حل المشاكل

### المشكلة: التطبيق لا يعمل على Render

**الحل:**
1. تحقق من Logs في Render
2. تأكد من Environment Variables صحيحة
3. تأكد من `package.json` يحتوي على `"start": "node server.js"`

### المشكلة: لا يمكن الاتصال بقاعدة البيانات

**الحل:**
1. تأكد من Firewall في Azure يسمح بـ Azure services
2. جرّب الاتصال من Azure Data Studio أولاً
3. تحقق من DB_SERVER ينتهي بـ `.database.windows.net`

### المشكلة: "Application Error" في Render

**الحل:**
```javascript
// تأكد من server.js ينتهي بـ:
const PORT = process.env.PORT || 3000;
app.listen(PORT, '0.0.0.0', () => {
    console.log(`Server is running on port ${PORT}`);
});
```

---

## 🎉 تم بنجاح!

الآن مشروعك متاح على الإنترنت! 🚀

**الرابط:** https://ihh-system.onrender.com

**شارك الرابط مع:**
- الأستاذ للتقييم
- زملائك للاختبار
- أي شخص في العالم!

---

## 📚 موارد إضافية

- [Render Documentation](https://render.com/docs)
- [Azure SQL Documentation](https://docs.microsoft.com/azure/sql-database/)
- [GitHub Guides](https://guides.github.com/)
- [Node.js Best Practices](https://github.com/goldbergyoni/nodebestpractices)

---

**تم إعداد هذا الدليل بواسطة:** GitHub Copilot
**التاريخ:** 22 ديسمبر 2025
